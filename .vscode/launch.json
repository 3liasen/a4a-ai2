{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "PHP Current File",
      "type": "php",
      "request": "launch",
      "program": "${file}",
      "cwd": "${workspaceFolder}"
    }
  ]
}
